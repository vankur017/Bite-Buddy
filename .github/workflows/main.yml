name: Secret Scanning Report

on:
  schedule:
    - cron: "0 8 * * *"   # Runs daily at 08:00 UTC
  workflow_dispatch:       # Allow manual trigger from Actions tab

permissions:
  contents: read
  security-events: read

jobs:
  fetch-alerts:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Fetch open secret scanning alerts (JSON)
        run: |
          curl -s -H "Accept: application/vnd.github+json" \
               -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
               https://api.github.com/repos/${{ github.repository }}/secret-scanning/alerts \
               > secret-alerts.json
          cat secret-alerts.json   # 👈 debug print
          echo "Saved alerts to secret-alerts.json"

      - name: Convert JSON to CSV
        run: |
          echo "id,secret_type,state,resolution,created_at,url" > secret-alerts.csv
          jq -r '.[] | [
            .number,
            .secret_type,
            .state,
            (.resolution // "unresolved"),
            .created_at,
            .html_url
          ] | @csv' secret-alerts.json >> secret-alerts.csv
          echo "Saved alerts to secret-alerts.csv"
          cat secret-alerts.csv   # 👈 debug print

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: secret-scanning-alerts
          path: |
            secret-alerts.json
            secret-alerts.csv
